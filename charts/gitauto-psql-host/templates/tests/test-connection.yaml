apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitauto-psql-host.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "gitauto-psql-host.name" . }}
    helm.sh/chart: {{ include "gitauto-psql-host.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gitauto-psql-host.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
