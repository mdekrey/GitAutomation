apiVersion: v1
kind: Service
metadata:
  name: {{ template "gitautomation.fullname" . }}
  labels:
    app: {{ template "gitautomation.name" . }}
    chart: {{ template "gitautomation.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "gitautomation.name" . }}
    release: {{ .Release.Name }}
