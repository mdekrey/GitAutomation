version: '3.1'

services:
  gitautomation:
    image: gitautomation
    build:
      context: ./GitAutomation
      dockerfile: Dockerfile
    secrets:
      - gitcredentials
      - configuration.json
    volumes:
      - "./GitAutomation.AllAddons/obj/Docker/publish:/extra-bins/z-addons-release"
    ports:
      - "32768:80"

  gitautomation-psql:
    image: gitautomation-psql
#    build:
#      context: ./GitAutomation.Postgres
#      dockerfile: Dockerfile
#    ports:
#      - "5432"
#    secrets:
#      - sqlcredentials
#      - sqleula


secrets:
  gitcredentials:
    file: ./git-credentials.txt
  configuration.json:
    file: ./configuration.json
